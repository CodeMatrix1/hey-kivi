# Chat UI (`web/chat/`)

Browser UI served by FastAPI at `/` with static assets under `/static/chat/…`.

Query library loads `web/assets/query_cases.json` (sync via `python -m hindsight_pipeline_2.evals.sync_cases`).
