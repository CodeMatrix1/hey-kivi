/** Max message length for auto-extracting reminder content from chat. */
export const REMINDER_CONTENT_MAX_LENGTH = 200;

/** Active refresh window before dueAt (ms). */
export const REMINDER_ACTIVE_WINDOW_MS = 5 * 60 * 1000;

/** Active refresh interval while within window (ms). */
export const REMINDER_ACTIVE_INTERVAL_MS = 60 * 1000;

/** Toast visibility duration (ms). */
export const REMINDER_TOAST_DURATION_MS = 10 * 1000;
